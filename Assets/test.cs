using System.Collections;
using System.Collections.Generic;
using UnityEngine.SceneManagement;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //int temp = SceneManager.sceneCountInBuildSettings - 1;
        //print("this is " + temp);
        // print(PlayerPrefsManager.GetMasterVolume());
        //PlayerPrefsManager.SetMasterVolume(0.3f);
       //  print(PlayerPrefsManager.GetMasterVolume());

       // print(PlayerPrefsManager.GetDifficulty());
       // PlayerPrefsManager.SetDifficulty(0.4f);
       // print(PlayerPrefsManager.GetDifficulty());

        // PlayerPrefsManager.DeleteKeys();
    }

    void Update()
    {
        
    }
}
